# UART-simulation
A simple uart communication protocol simulated between two nodes using vivado software.
A UART_transmitter send the data which is recieved by the UART_reciever both of which are synchronized using a clock. The programming language used is VHDL
